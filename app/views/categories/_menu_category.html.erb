<br><br>
<% if menu_category.id == params[:category].to_i %>
	<%= menu_category.name %>
<% else %> 
<%=link_to menu_category.name, :controller => "events", :action => "index", :category => menu_category.id %>
<% end %>
