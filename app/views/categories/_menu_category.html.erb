
<br><br>
<% if menu_category.id == params[:id].to_i && params[:controller] == "categories" %>
	<%= menu_category.name %>
<% else %> 
<%=link_to menu_category.name, :controller => "categories", :action => "show", :id => menu_category.id, :type => @type.id %>
<% end %>
