<% if category.id == params[:id].to_i  && params[:controller] == "categories" %>, <%= category.name%><% else %>, <%=link_to category.name, :controller => "categories", :action => "show", :id => category.id, :type => @type %><% end %>