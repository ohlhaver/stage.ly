<% provide :title, @artist.name + " in Lübeck" if @artist %>
<% provide :page_title, @artist.name + " in Lübeck" if @artist %>
<% provide :keywords, @artist.name %>

<div class="hidden-phone">
	<%= render :partial => 'events/label_bar'%>
</div>
</div>
	<table class="table table-striped">
	  <tbody>
		<%= render @events %>
	  </tbody>
	</table>
