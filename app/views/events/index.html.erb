<% provide :title, @category.name + " in Berlin" if @category %>
<% provide :page_title, @category.name + " in Berlin" if @category %>
<% provide :keywords, @category.name if @category %>

	<div class="span4">
		Künstler
	</div>
	<div class="span2">
		Komponisten
	</div>
	<div class="span2">
		Ort
	</div>
	<div class="span2">
		Beginn
	</div>
	<%= render @events %>

